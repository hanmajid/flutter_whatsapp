import 'package:flutter/material.dart';

class WhatsappWebScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('WhatsappWebScreen'),
      ),
      body: Center(
        child: Text('TODO'),
      ),
    );
  }

}