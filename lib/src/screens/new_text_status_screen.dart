import 'package:flutter/material.dart';

class NewTextStatusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NewTextStatusScreen'),
      ),
      body: Center(
        child: Text('TODO'),
      ),
    );
  }

}